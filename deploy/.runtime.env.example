# Copy to /opt/stone-app/.runtime.env on the VPS and replace every placeholder.
# Keep this file outside the repository after configuration.
AWS_ACCESS_KEY_ID=replace-with-runtime-access-key-id
AWS_SECRET_ACCESS_KEY=replace-with-runtime-secret-access-key
